// undefined - declare the variable but no have assigned value
var x;
console.log(x); // undefined

let b;
console.log(b); // undefined

typeof(undefined) // undefined

// null
typeof(null) // object

var c = null;
console.log(c); // null

// Example: You can assing null to a variable and
// wait to the service response if this variable is !null will have response.
